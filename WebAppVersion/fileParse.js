'use strict';
const sqlize = require('sequelize');

function fileParse(FILENAME) {
    // FIELDS
    var db;
    var fileName = FILENAME;

    // FINISH
    db = new sqlize("Expenses");
    
        

}
module.exports = fileParse;
